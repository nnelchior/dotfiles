import QtQuick
import Quickshell

Rectangle {
    id: root
    enum Size { XS, S, M, L, XL }
    enum Shape { Round, Square }
    enum ColorStyle { Filled, Tonal, Outilned, Standard }
    enum Width { Default, Narrow, Wide }
    enum Variant { Default, Toggle }
}
